name: Deploy Backend

on:
  push:
    branches:
      - master

jobs:
  deploy-backend:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: docker compose down
      - run: docker compose up -d --build
      - run: docker ps